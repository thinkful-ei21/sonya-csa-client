import React from 'react';

export default function Nav() {
  return(
    // eslint-disable-next-line
    <nav role='navigation'>
      <header>
        <h1>Twisted Root Farm</h1>
      </header>
    </nav>
  )
}